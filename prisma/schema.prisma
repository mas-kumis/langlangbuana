generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model Promo {
  id    String  @id @default(uuid()) @map("_id")
  title String
  price String?
  desc  String
}
